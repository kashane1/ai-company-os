# Polish Session — life-clock — 2026-05-05

## Mode

`freeform-polish` — but the operator's payload was a feature proposal
(more plan choices, history-aware defaults, Pro-gated picker). Per the
skill's decision matrix this is Feature-tier work, so the cycle opened
with a batched Ask before any code was written. Operator answered
1A / 2A / 3B / 4 one-shot, authorizing the build.

## Iterations

- [00:57] 872ed6d — feat(life-clock): history-aware step target — Stretch — QuestEngine
- [01:09] 36b2785 — feat(life-clock): expose 3 quest variants per category — Stretch — QuestEngine
- [01:29] dcabab1 — feat(life-clock): one-shot today's plan overrides — Feature (operator-authorized) — LifeClockStore
- [01:37] 9b769fe — feat(life-clock): plan editor sheet with Pro lock — Feature (operator-authorized) — TodayView + new PlanEditorSheet
- [01:55] 8db9fa1 — fix(life-clock): drop nutrition slot on heavy-alcohol day — Polish (regression fix) — QuestEngine

## Stretch decisions (operator review)

- 872ed6d — chose p50 × 1.10 over a more elaborate adherence-weighted
  model (operator's Ask 2 reply). Rounded to 500, floored at 5,000,
  capped at 20,000. Falls back to the historical 7,500 default when
  fewer than 5 days of history exist (so brand-new users still see a
  sensible target, not p50-of-noise).
- 36b2785 — three categories (movement / sleepRecovery / nutritionHabit)
  per operator's Ask 1 reply. Sleep+recovery share a slot because both
  are "wind down / reset"; nutrition+habit share because both are
  intake-shaped. Each variant pool returns up to 3 distinct slugs,
  default-first.

## Asks

### Resolved this session

- "Categories — 3 distinct slots, or broader 4-cat scheme?" → 1A (3 slots) → 36b2785
- "How smart should defaults be?" → 2A (rolling p50 ±10%) → 872ed6d
- "Free-user UX for the picker?" → 3B (lock badge → paywall) → 9b769fe
- "Persistence: long-term preferences or one-shot?" → one-shot → dcabab1
- "Heavy-alcohol day: do nutrition + recovery now both surface, or
  does recovery still suppress nutrition like the prior single-slot
  engine?" → operator-implicit (test asserted suppression) → 8db9fa1

### Outstanding (cycle-end batch)

None.

## Regressions caught

- DietAlignmentTests/testHeavyAlcoholStillTakesPriorityOverNutrition:
  unintended diff — splitting the third slot into sleepRecovery +
  nutritionHabit caused both to emit on heavy-alcohol days. Reverted
  via 8db9fa1; nutrition slot now drops out when the recovery quest
  fires.
- Confirmed pre-existing failures (NOT introduced by this session,
  verified by running on parent commit edd6715):
  - LifeClockE2ETests/testFullLoopFromOnboardingThroughColdRestart
  - LifeClockStoreTests/testQuestUndoRemovesLedgerEntryAndCompletionState
  - LifeClockStoreTests/testCompletedPlanRestoresAcrossColdRestart
  - LifeClockStoreTests/testEveningLogDoesNotTriggerSuppressionPath
  - LifeClockStoreTests/testQuestCompletionSurvivesTitleRename
  - SubscriptionStoreTests/testPurchase…, testRestore…, testRevocation…
  - These are not regressions from this session; flagged here so a
    follow-up pass can triage them.

## A11y identifiers added

- today.planEdit (Pro user's edit affordance)
- today.planEditLocked (free user's lock badge)
- planEditor.screen
- planEditor.done
- planEditor.reset
- planEditor.category.{movement|sleepRecovery|nutritionHabit}
- planEditor.option.{slug}

## Vision updates

- Open Questions appended: none this session
- Decided constraints proposed: none this session

## Next pass

- Triage the pre-existing failures listed above (separate cycle —
  they're not gating this PR but they do mean main isn't green).
- Add UITests for the picker flow once the pre-existing flakes are
  cleaned (XCUITest assertions on `planEditor.option.*`).
- Consider promoting one-shot picks to long-term preferences in a
  Settings surface (operator deferred this for v1).
