# design-studio (routing pointer)

This is a thin routing pointer, not the skill content. To run the premium design
track on a single chosen build (art-direction packet → build → desktop/mobile
screenshots → visual rubric review), read and follow:

→ `skills/adapters/claude/design-studio.md`

which implements the canonical definition at
`skills/canonical/design-studio/skill.md`. Do not add skill logic here.
