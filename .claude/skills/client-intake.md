# client-intake (routing pointer)

This is a thin routing pointer, not the skill content. To capture a local-SMB
client's business details into a structured brief and a localized site context,
read and follow the adapter:

→ `skills/adapters/claude/client-intake.md`

which implements the canonical definition at
`skills/canonical/client-intake/skill.md`. Do not add skill logic here.
