# Infra DB

This directory is reserved for database migrations, seeds, and related setup.

Likely future contents:

- migration configuration
- versioned schema changes
- seed data for local development

V1 keeps the directory present so durable state has an obvious home.
